{
  "name": "@ex-machina/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared utils package",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests --only-failures",
    "build": "echo \"no build needed\" && exit 0",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@ex-machina/configs": "workspace:*"
  }
}
