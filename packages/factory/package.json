{
  "name": "@ex-machina/factory",
  "version": "0.0.0",
  "private": true,
  "description": "Portable software factory definitions and validation",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests",
    "build": "echo \"no build needed\" && exit 0",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@ex-machina/configs": "workspace:*",
    "@types/bun": "^1.2.22",
    "typescript": "^6.0.2"
  }
}
